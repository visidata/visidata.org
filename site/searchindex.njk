---
permalink: '/searchindex.json'
---
{
  "docs": [
    {% for item in collections.docs %}
      {
        "id": "{{ item.url }}",
        "title": "{{ item.data.eleventyNavigation.key }}",
        "body": "{{ item.templateContent | safe }}"
      }{% if not loop.last %},{% endif %}
    {% endfor %}
  ]
}
